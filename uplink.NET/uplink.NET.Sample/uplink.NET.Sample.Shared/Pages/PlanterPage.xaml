<Page
    x:Class="uplink.NET.Sample.Shared.Pages.PlanterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:uplink.NET.Sample.Shared.Pages"
    xmlns:controls="using:uplink.NET.Sample.Shared.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" Background="{StaticResource StorjBlue}" OverflowButtonVisibility="Collapsed">
            <CommandBar.Content>
                <TextBlock x:Uid="Planter_Title" Foreground="White" FontSize="25" Margin="10,0"/>
            </CommandBar.Content>
            <AppBarButton x:Uid="Planter_Back" Foreground="White" Command="{Binding GoBackCommand}">
                <AppBarButton.Icon>
                    <BitmapIcon UriSource="ms-appx:///Assets/storj_left.png"/>
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <controls:PlanterControl
                x:Name="PlanterView"
                MinWidth="400"
                MinHeight="300"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Margin="10"/>
        </ScrollViewer>
    </Grid>
</Page>
